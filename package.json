{
  "name": "reading-list-news",
  "version": "1.0.0",
  "private": true,
  "description": "Static reading list of recent articles from selected sources.",
  "type": "module",
  "scripts": {
    "build": "node scripts/generate.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "rss-parser": "^3.13.0"
  }
}
